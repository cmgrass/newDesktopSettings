" vimrc for grasscm Ubuntu Desktop 2004
" 20200803

" Display line numbers
set number

" Syntax Highlighting
if &diff
    syntax off
else
    syntax enable
endif

" solarized settings
set background=dark
colorscheme solarized
